2026_02_20_roadmap.md

I'd like to stop bouncing between entrypoint and the game to adjust the window.
What needs to happen to give control to the game?
So much initialization needs to wait for all of the subsystems to be ready.
To do a good job, I need to state the value:
	"main_game" is the entrypoint.
Then from there I can get rid of main_entrypoint?
	game_init == main

Maybe there's a "default_main" which loads GL and all that,
	if the user wants to plug in Vulkan or whatever,
	then they can do that by writing a custom main.

Bottom Line: This needs design, so I need to refocus on my task.
--------------------------------------------------------------------------------
I noticed the web build canvas is too large. I need to size it appropriately.
For now, I'll check the RES_X, RES_Y and RES_SCALE properties to compute canvas size.
Same as the window size is computed.
In create_window on JS, I'll just set the canvas element's size?